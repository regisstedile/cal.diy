export function restorePromptComplexity(prompt: string | null | undefined): string {
  return prompt ?? "";
}

export function cleanPromptForDisplay(prompt: string | null | undefined): string {
  return prompt ?? "";
}
